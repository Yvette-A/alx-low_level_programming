/**
 * _abs - computes the absolute value of and integer
 * @x: integer to be tested
 * Return: the absolute value
 */
#include "main.h"
int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
