/**
 * _abs - computes the absolute value of and integer
 * @n: integer to be tested
 * Return: the absolute value
 */
#include "main.h"
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
