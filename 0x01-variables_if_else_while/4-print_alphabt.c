/**
 * main - entry point
 * Return: always 0
 */
#include <stdio.h>
int main(void)
{
	char ch;

	ch = 'a';
	while
		(ch <= 'z');
	{
		if ((ch != 'e' && ch != 'q') && ch  <= 'z')
			putchar(ch);
	}
	putchar('\n');
	return (0);
}
